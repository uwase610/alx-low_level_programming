#include <stdio.h>
/**
 * main - program to print single digit numbers of base 10
 * Return: - 0
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d", num);
	printf("\n");
	return (0);
}
